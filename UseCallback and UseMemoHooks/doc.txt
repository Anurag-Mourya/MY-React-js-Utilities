React - A JavaScript library for building user interfaces
                                                what React is 

React is a declarative, efficient, and flexible JavaScript library for building user interfaces. It lets you compose complex UIs from small and isolated pieces of code called “components”.

Declarative - React makes create interactive UIs.Declarative views make your code more predictable and easier to debug

Component-Based - Build encapsulated components that manage their own state

Learn Once, Write Anywhere - you can develop new features in React without rewriting existing code.

A Component Using External Plugins - React allows you to interface with other libraries and frameworks. 


                                         Create React App

 You’ll need to have Node >= 14.0.0 and npm >= 5.6 on your machine

NPM - (Node packege manager)- Npm is a tool that use to install whole packages.Packages used by npm are installed globally.	

NPX(Node Packege Execute)- Npx is a tool that use to execute  packages.Packages used by npx are not installed globally.

Babel 7.20 - Babel is a JavaScript compiler.
Babel is a toolchain that is mainly used to convert ECMAScript 2015+(ES6) code into a backwards compatible version of JavaScript  that can run all the browsers easly.
Babel can convert JSX syntax! in regular js..